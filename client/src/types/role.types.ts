export interface CreateRoleResponse {
	code: string;
	name: string;
}

export interface RoleResponse {
	id: number;
	CreatedAt: string;
	UpdatedAt: string;
	code: string;
	name: string;
}
